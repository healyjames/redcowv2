# Coding Guidelines

> Fill this in — or run `/clarify coding` to be quizzed and have it generated. The `code-review` skill
> reads this. **Encode decisions, not generic best-practices** — Claude already writes clean code by
> default, so only list what's specific to you or non-obvious.

## Language & style decisions

<!-- Only the project-specific or non-default ones. Examples:
     - `type` aliases over `interface`.
     - Immutability: array methods over mutation; avoid `let` for accumulation.
     - Early returns over nested if/else.
     - Options objects over positional params past ~3 args.
     - `unknown` over `any`; no non-null `!`; no `@ts-ignore` without an explaining comment. -->
- <decision>
- <decision>

## Error handling

<!-- The specific pattern. e.g. "wrap external calls in `withRetry`; surface failures via `AppError`,
     never raw exceptions." -->
- <...>

## Naming & structure

<!-- Naming conventions, file/module layout rules that reviewers should enforce. -->
- <...>

## Banned / discouraged

<!-- Patterns to reject in review, and why. -->
- <pattern> — <why>
